DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-a-long-random-secret"

# Email - SMTP
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="Chennai Movies <no-reply@example.com>"

# Optional: set to true only when you have configured a real payment provider.
PAYMENT_MODE="mock"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
